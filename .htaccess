RewriteEngine On

# Se o arquivo ou pasta existir, carrega normalmente
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Caso contrário, redireciona para o React
RewriteRule ^ index.html [L]